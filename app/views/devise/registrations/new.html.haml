- breadcrumb :login

.row
  .wrapper.col-md-12
    %h2 ボケてアカウント新規登録
    %p ボケてでは色々な人に「ボケて」もらって「評価」してもらいたいと思っています。投稿したボケが、誰かの辛い時間を楽しい時間に変えることもできるし、面白い！と思うボケにコメントしたことがボケ投稿者をハッピーにすることが出来ます

    .registration
      .login-form-content
        = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
          = f.error_notification
          .form-inputs
            = f.input :email, required: true, autofocus: true, placeholder: 'メールアドレス', input_html: {class: 'form-control input-lg'}
            = f.input :password, required: true, input_html: {class: 'form-control input-lg'}
            = f.input :password_confirmation, required: true, input_html: {class: 'form-control input-lg'}
          .form-actions.text-center
            = f.button :submit, "送信", class: 'btn btn-warning btn-lg'

  .scroll-to-top.text-right.clearfix
    %a.btn.btn-warning ページのトップへ戻る
