- breadcrumb :login

.row
  #wrapper.col-md-12
    %h2 ボケてにログイン

    .registration
      .login-form-content
        = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
          .form-inputs
            = f.input :email, autofocus: true, placeholder: 'メールアドレス', input_html: {class: 'form-control input-lg'}
            = f.input :password, placeholder: 'パスワード', input_html: {class: 'form-control input-lg'}
          .form-actions.text-center
            = f.button :submit, "ログイン",  class: 'btn btn-warning btn-lg'

    .login-description
      %p
        パスワードを忘れた場合は
        = link_to 'パスワード再発行フォーム', '#'
        より、新規パスワードを登録してログインしてください。ボケてのアカウントをまだ取得していない方は
        = link_to '新規登録', '#'
        から新しく登録をお願いします。登録・利用ともに完全無料です。

  .scroll-to-top.text-right.clearfix
    %a.btn.btn-warning ページのトップへ戻る


