- breadcrumb :accounts

.row
  .col-md-9
    = render 'tabs'

    .wrapper
      = render 'category_tab_menu'
      %h2 プロフィール写真のアップロード・変更

      .gray-box
        .preview
          %img#previewImg{src: '', style: 'display:none'}

        = simple_form_for @user, url: accounts_path, method: :patch do |f|
          %a.btn.btn-default.pickup-file ファイルを選択
          = f.input :avatar, as: :file, label: false
          = f.submit 'アップロード', class: 'btn btn-warning'


  .col-md-3.no-padding
    = render 'widget'